workflows:
  ios-workflow:
    name: iOS Workflow
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: MoodTrackerKey2
    environment:
      xcode: latest
      cocoapods: default
    scripts:
      - name: Install pods
        script: |
          cd src
          pod install || true
      - name: Fetch provisioning profile
        script: |
          app-store-connect fetch-signing-files "com.moodtracker.daily" --type IOS_APP_STORE --create --skip-certificate
      - name: Set up code signing
        script: |
          cd src
          xcode-project use-profiles
      - name: Build iOS app
        script: |
          cd src
          xcode-project build-ipa --project "Mood Tracker.xcodeproj" --scheme "Mood Tracker"
    artifacts:
      - build/ios/ipa/*.ipa
